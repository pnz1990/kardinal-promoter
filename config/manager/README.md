# This directory intentionally left empty.
# kardinal-promoter is deployed via the Helm chart (chart/kardinal-promoter/).
# Raw controller manifests are not maintained here.
# See: make install (uses Helm) or make docker-build for container builds.
