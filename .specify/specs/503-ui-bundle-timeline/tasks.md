# Tasks: 503-ui-bundle-timeline

## Tasks

- [ ] Read existing `BundleTimeline.tsx`, `types.ts`
- [ ] Implement full bundle row: version chip, per-env status chips, promoted-by, promoted-at age
- [ ] Link each env chip to PR URL if available
- [ ] Dim superseded/failed bundles
- [ ] Add pagination: show 20 by default, "Load more" for rest
- [ ] Show override indicator if bundle has override records
- [ ] Wire Timeline tab into pipeline detail view
- [ ] Write vitest tests for row rendering and pagination
