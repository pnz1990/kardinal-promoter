# Tasks: 500-ui-pipeline-ops-view

## Tasks

- [ ] Read existing `PipelineList.tsx` and `types.ts` to understand current data
- [ ] Add columns: Age (staleness), Blocked (gate count), Interventions
- [ ] Add sort-by-column support (state preserved across polls)
- [ ] Add colored row health indicator using `HealthChip` patterns
- [ ] Float blocked pipelines to top by default
- [ ] Add vitest tests for sort logic and column rendering
- [ ] Verify no table jump on poll refresh
