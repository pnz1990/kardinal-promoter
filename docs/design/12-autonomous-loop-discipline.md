# 12: Autonomous Loop Discipline — Zero Human Dependencies in Steady State

> Status: Active | Created: 2026-04-18
> Applies to: kardinal-promoter agent sessions

---

## What this does

Defines the loop behavior that eliminates human-gated PR queues. The agent must
merge everything it produces without waiting. Human involvement is only valid for
three specific scenarios — everything else the agent resolves autonomously.

---

## The three valid `[NEEDS HUMAN]` scenarios

Everything outside these three is an agent failure, not a design constraint:

1. **Token has no admin rights** — the GitHub token cannot modify branch protection.
   Symptom: 403 on `PATCH repos/{owner}/{repo}/branches/main/protection`.
   Fix: configure the token with admin rights. Until fixed: post once, do not repeat.

2. **Genuine value judgment required** — the agent cannot determine correctness
   without product knowledge the human hasn't encoded yet. Example: two competing
   architectural approaches where both are valid and the choice has long-term implications.
   This is rare. Most "I'm not sure" situations are solvable with more reading.

3. **CI has been red for >24 hours and the root cause requires external access** —
   the agent cannot diagnose a failing CI step because it requires access to a
   credential, external service, or environment the agent cannot reach.

Everything else — merge conflicts, branch protection, test failures, spec ambiguity,
review requirements — the agent resolves autonomously.

---

## Autonomous merge protocol (in priority order)

When `gh pr merge $PR_NUM --squash --delete-branch` fails:

### Step 1 — Try `--admin`
```bash
gh pr merge $PR_NUM --repo $REPO --squash --delete-branch --admin
```
If this fails with "Pull request is not mergeable": check CI, fix, retry.
If this fails with "Must be approved by someone other than the author" or similar:
proceed to Step 2.

### Step 2 — Disable `enforce_admins`, merge, re-enable
```bash
# Disable — returns 200 if token has admin rights, 403 if not
RESULT=$(gh api -X PATCH "repos/$REPO/branches/main/protection" \
  --field enforce_admins=false 2>&1)
if echo "$RESULT" | grep -q '"enforce_admins":.*false\|200'; then
  gh pr merge $PR_NUM --repo $REPO --squash --delete-branch --admin
  # Always re-enable, even on merge failure
  gh api -X PATCH "repos/$REPO/branches/main/protection" --field enforce_admins=true
else
  echo "[QA] Cannot disable enforce_admins (403) — token lacks admin rights"
  # This is valid [NEEDS HUMAN] scenario 1. Post once and move on.
fi
```

### Step 3 — If branch protection requires a specific reviewer account

This means the project requires a second GitHub account. This is valid scenario 1.
Post `[NEEDS HUMAN: merge-blocked — branch protection requires reviewer with write access.
Token: $(gh api user --jq .login). Branch protection: require_code_owner_reviews=$(gh api
repos/$REPO/branches/main/protection --jq .required_pull_request_reviews.require_code_owner_reviews)]`

Do not post `[NEEDS HUMAN]` for any other merge failure scenario.

---

## Queue generation discipline

When the agent is about to generate a new queue, it first checks:

```bash
# Count in-review items that the agent cannot currently merge
BLOCKED_COUNT=$(python3 -c "
import json
s = json.load(open('.otherness/state.json'))
print(len([d for d in s.get('features',{}).values() 
          if d.get('state') == 'in_review']))
")
```

If `BLOCKED_COUNT >= 3`: do not generate new work. Attempt to merge existing
in-review items using the autonomous merge protocol. If all are genuinely unmergeable
(scenario 1), enter standby. Do not generate more work that will also be unmergeable.

---

## Present (✅)

- ✅ **qa.md §3e — 3-step autonomous merge protocol** (`_merge_pr()` function):
  normal → `--admin` → clear branch protection (PUT) → restore. Never posts
  `[NEEDS HUMAN: pr-approval-required]` before attempting all three steps.
  `_RESTORE_TRAP` with `trap ... EXIT` ensures branch protection is always restored.
  Implemented in `~/.otherness/agents/phases/qa.md §3e`.

- ✅ **qa.md §3e — enforce_admins toggle error handling**: The `_RESTORE_TRAP`
  environment variable captures the restore curl command; `trap "$_RESTORE_TRAP" EXIT`
  fires on both normal exit and error. Explicit `eval "$_RESTORE_TRAP"` + `trap - EXIT`
  on normal exit. Implemented alongside the 3-step protocol in qa.md §3e.

- ✅ **coord.md §1c — queue gate (in_review ≥ 3)**: When total `in_review` items ≥ 3,
  queue generation is skipped and the queue-gen lock is released immediately. Prevents
  saturating the review queue when items accumulate. Implemented in coord.md §1c.

- ✅ **standalone.md HARD RULES — `[NEEDS HUMAN]` as last resort**: The HARD RULES
  section states: "Before posting `[NEEDS HUMAN]` for any merge failure: attempt all 3
  steps in qa.md §3e. Only post `[NEEDS HUMAN]` when step 3 fails with a specific error
  (403 = no admin rights). Log the exact error." Implemented in standalone.md §HARD RULES.

- ✅ **SM §4f-pdca — PDCA workflow result check**: SM §4f now runs
  `gh run list --workflow=pdca.yml --limit 1 --json conclusion --jq '.[0].conclusion'`
  after CI/NEEDS_HUMAN checks. If conclusion=failure: sets HEALTH=RED, writes
  `pdca_status=failure` to state.json, and opens a dedup-guarded `[PDCA FAILING]` issue
  with `needs-human,area/test,priority/critical`. Batch report headline now includes
  `pdca: PASS|FAIL|unknown` so operators see journey health at a glance. Fail-open:
  API errors leave HEALTH unchanged. (PR #1045, 2026-04-22)

- ✅ **SM §4f-regression — PDCA regression attribution + COORD §1c-pdca-gate**: When PDCA
  transitions from passing to failing, SM §4f-regression runs `git log` over `pkg/`, `cmd/`,
  `chart/`, `web/src/` since the last git tag, posts `[PDCA REGRESSION — attributed commits]`
  to REPORT_ISSUE with a needs-human issue, and writes `pdca_status=failure` to state.json.
  COORD §1c-pdca-gate reads `pdca_status` before queue generation — if `failure`, it skips
  queue generation and item claiming entirely, posting one COORDINATOR HALTED comment.
  `pdca_regression_posted` flag prevents re-posting on every cycle while regression persists.
  Cleared when PDCA passes again (`pdca_status=success`). (PR #1061, 2026-04-22)

## Future (🔲)

- 🔲 **Simulation delta feedback is one-directional: SM posts it, COORDINATOR never reads it** — the `Loop prediction → behavior feedback loop` item says the SM should post `predicted: N, actual: M (ratio: M/N)` in every batch report. That is the write half of the feedback loop. The read half is absent: the COORDINATOR does not read the prior SM delta before generating the next queue. If actual/predicted < 0.5 for 3 consecutive batches, the COORDINATOR should *reduce queue depth* (pick 1–2 items instead of 3–4) until the ratio recovers. If ratio > 1.2 consistently, the COORDINATOR should *increase queue depth*. Without the COORDINATOR reading the delta, the simulation is a reporting artifact, not a calibration mechanism. Concrete implementation: the COORDINATOR's queue-generation step must check the last SM batch report on Issue #1 for `predicted:` / `actual:` fields, compute the rolling ratio, and adjust the queue size accordingly. Until this is implemented, "simulation predictions are not visibly changing agent behavior" remains true regardless of how accurately the SM computes and posts the delta. ⚠️ Inferred from pressure lens: the second half of the prediction-to-behavior feedback loop does not exist.

- 🔲 **Skills inventory is never consulted before starting work — proactive skills discovery is absent** — the `Skills inventory` item (`docs/aide/skills-inventory.md`) creates a machine-maintained index of encoded patterns. But there is no step in any agent's workflow that checks this inventory at session start and loads applicable skills for the work about to be done. A COORDINATOR generating a queue for a `feat(scm):` item will not check whether a "scm-provider-pattern" skill exists and load it. An ENGINEER implementing a reconciler change will not check whether a "reconciler-idempotency" skill was encoded from a prior batch. The gap: skills exist in `~/.otherness/agents/` but are only loaded when a human invokes `/skill <name>`. Add a session-startup step to the COORDINATOR agent: before generating the queue, read `docs/aide/skills-inventory.md` (if it exists), identify skills matching the work area (by keyword match against the queue items), and emit `[SKILL SUGGESTED: <name> — relevant to <area>]` for each match. Engineers should check this suggestion list at the start of implementation. Without proactive skills discovery, the skills library is a write-only artifact — patterns are encoded but never applied, and the agents are no smarter than they were before `otherness.learn` ran. ⚠️ Inferred from pressure lens: "agents are not meaningfully smarter than they were two weeks ago" — skills exist but are not consumed.

- ✅ **Loop honesty signal: housekeeping_streak counter in SM §4f-housekeeping-streak**: SM §4f-housekeeping-streak scans PRs merged in the last 2 hours and counts "substantive" PRs (feat/fix/refactor/test with file changes in `pkg/`, `cmd/`, `web/src/`, or `chart/`). If 0 substantive PRs: increments `housekeeping_streak` in state.json and posts `[LOOP STALL — N consecutive housekeeping-only batches]` to REPORT_ISSUE when streak > 3 (dedup: skips if posted in last 6 comments). If substantive PRs shipped: resets streak to 0. COORD §1b-preflight reads `housekeeping_streak` from state.json and sets `COORD_ACTION=vision-first` when ≥ 3, triggering vibe-vision-auto before queue-gen. (PR #1062, 2026-04-22)

- ✅ **Self-improvement tracking: skills library growth rate**: SM §4f-skills-tracking computes `SKILLS_COUNT` (count of `.md` files in `~/.otherness/agents/skills/` excluding PROVENANCE.md and README.md) and `SKILLS_LAST_ADDED` (most recent `## YYYY-MM-DD` header in PROVENANCE.md). Both are added to the SM batch report `<details>` block: `skills: N (last added: YYYY-MM-DD)`. If `last_added` is more than 14 days ago: appends ` ⚠️ No new skill in Xd — run /otherness.learn` to the line. The SM §4c cadence enforcement already opens a priority/high issue and escalates stale learns. (PR #1063, 2026-04-22)

- 🔲 **Loop prediction → behavior feedback loop** — the simulation exists (`otherness.simulate`) but its predictions are not visibly changing agent behavior. The SM should compare the predicted item completion count from the last simulation against actual items completed. If actual < predicted * 0.5 consistently, something is miscalibrated. Post the delta in every SM batch report: `predicted: N items, actual: M items (ratio: M/N)`. Persistent underdelivery is a system signal, not a one-off.

- 🔲 **Workflow self-healing: detect and recover from syntax-error regression** — when the scheduled workflow fails at a pre-agent step (Install/Checkout/Auth) for N consecutive runs, the system has no mechanism to detect or recover. The agent never runs, so it cannot self-diagnose. Add an out-of-band canary: a lightweight daily GitHub Actions check (separate, minimal workflow with no otherness dependency) that: (1) runs `bash -n` on each `run:` block in `otherness-scheduled.yml`; (2) posts a `[WORKFLOW BROKEN]` comment to Issue #1 if any syntax check fails; (3) opens a `needs-human` issue with the failed step name and the bash error. This creates a separate watchdog that survives a broken main workflow.

- 🔲 **Otherness onboarding quality gate: `/otherness.onboard` output review** — a new project added via `/otherness.onboard` produces docs (otherness-config.yaml, workflow YAML, design doc stubs) that still require manual editing before the loop runs successfully. The onboarding is "close but not zero-touch." The gap is systematic: the tool generates the scaffolding but does not validate it (no `bash -n` on the generated workflow, no `gh api` call to verify the generated secret names exist, no dry-run of the first scheduled run). Add a post-onboard validation step to the onboard agent: after generating all files, run the same preflight checks the workflow would run, and surface any gaps as `[ONBOARD GAP]` items the human must fix. The human should see a clear "3 gaps found, fix these before your first run" summary, not discover them after the first failed scheduled run.

- 🔲 **SM health state definition: explicit thresholds for GREEN/RED/STALL** — the SM posts a health signal after each batch but the criteria for GREEN vs RED vs STALL are implicit and subjective. A batch where CI passes but no feature PR merged can still be reported as GREEN. Define precise, machine-checkable thresholds: GREEN = at least one feat/fix/test/docs PR merged AND PDCA ≥ 80%; STALL = 3+ consecutive runs with zero substantive PRs; RED = workflow failed (no agent ran) OR PDCA = 0. These thresholds should be defined in `team.yml` (or this doc) and enforced by the SM `health_check` step rather than being judgment calls. Without explicit thresholds, the health signal is noise — it does not distinguish a productive run from a housekeeping-only run.

- 🔲 **Monoculture break: adversarial agent role for architecture reviews** — all sessions (COORDINATOR, ENGINEER-1..3, QA, SM, PM) share the same model and reasoning framework. When a design decision is made, it is reviewed by agents that reason identically to the one that made it. This is the monoculture problem: a systematic bias in one session propagates undetected. The flat DAG compilation failure (described in AGENTS.md) is the canonical example. Add an `ADVERSARY` session role whose sole function is to find the failure mode of any proposal before it is queued as an implementation item. The ADVERSARY asks "what would break this?" before COORDINATOR adds the item to the queue. Even if the ADVERSARY is the same model, the role prompt forces a different evaluation frame. The ADVERSARY session should be lightweight — it does not implement, it only challenges.

- 🔲 **Zero-PR session detection: agent ran but produced no mergeable content** — the SM currently detects housekeeping-only PRs but not the case where the agent ran for its full token budget and produced zero PRs at all (e.g., all items failed CI, all PRs were rejected by the merge protocol, or the coordinator generated a queue but no engineer picked up work). This is distinct from "session stall" — the workflow ran, the agent ran, tokens were consumed, but nothing shipped. Add detection: after each batch, if `gh pr list --merged --search "created:>1h ago"` returns 0 items, post `[SESSION DRY RUN — agent ran but shipped 0 PRs]` to Issue #1. Persistent dry runs (3+) should escalate to `[NEEDS HUMAN]` since the queue or merge protocol may be stuck in a way the agent cannot self-diagnose.

- 🔲 **Onboarding time-to-first-run metric: track and publish the setup duration** — the onboarding guide claims setup takes "under 30 minutes" but this is not measured. A new project added via `/otherness.onboard` has no record of how long it took from `gh repo create` to first successful scheduled run. Add a timestamp to the onboard output (`onboard_started_at`, `first_run_succeeded_at`) written to `otherness-config.yaml` or a `docs/design/00-onboarding.md` doc. The SM should report `time_to_first_run: Xmin` in the first 3 batch reports after a new project is onboarded. Without this metric, the claim "onboarding takes 30 minutes" is unverifiable and the improvement pressure from the vision scan has no baseline to push against.

- 🔲 **Self-feeding queue: COORDINATOR auto-triggers vision scan when queue empties** — when the work queue reaches zero items, the system currently stalls and waits for a human to run `/otherness.vibe-vision` or manually add direction. This is a reliability gap: a truly reliable loop ships at least one meaningful PR every run without exception, which requires work to always exist. When the COORDINATOR detects an empty queue at batch start, it should: (1) scan `docs/design/*/## Future` for unqueued `🔲` items; (2) convert the top N items (by document recency and priority) directly into queue items without human intervention; (3) if no Future items exist, invoke the vision scan agent inline to generate them. The human's role is to set *direction* (via vision pressure), not to manually re-seed the queue each time it empties. Without this, the loop's throughput is bounded by human attention span rather than queue depth.

- 🔲 **`otherness.learn` automatic invocation: skills extracted from every novel-pattern batch** — `otherness.learn` is designed to encode reusable patterns as skills, but it runs only when a human explicitly invokes it. This means most novel patterns (e.g., a new krocodile workaround, a new merge-conflict resolution, a new CEL expression pattern) are discovered in one session and lost in the next. The SM's end-of-batch step should check: "did this batch surface a pattern not present in any existing skill?" If yes, automatically invoke `otherness.learn` on the relevant artifact (PR diff, session log excerpt, error resolution). The SM does not need to judge what's "novel" — it can run `otherness.learn` on every batch where a non-trivial feat/fix merged, letting the skill agent decide whether to encode it. Without automatic invocation, the skills library grows at a rate of zero-per-batch unless a human explicitly notices and triggers it.

- 🔲 **Otherness setup guide completeness audit** — the setup guide (wherever it lives — `/otherness.onboard` output, `docs/`, otherness wiki) has not been independently audited for completeness. A new user following it step-by-step would encounter gaps: the OIDC role setup requires `setup-github-bedrock-key.sh` which is not in the guide; the `otherness-config.yaml` schema is not documented; the `team.yml` `agents_path` path-security rule is not mentioned. The gap: the guide was written to match one project's setup, not generalized for arbitrary new projects. Audit by simulating a cold-start on a fresh repo (not kardinal-promoter): follow every step in the guide exactly and record every point where the guide says "X" but the real system requires "Y". File each gap as a `[ONBOARD GAP]` item. Without this audit, "new project added today would still require significant human intervention" remains true regardless of how many quality-gate Future items are implemented.

- 🔲 **ADVERSARY activation gate: COORDINATOR must not queue an item without an ADVERSARY pass** — the `Monoculture break` Future item above describes adding an ADVERSARY session role. But it has no activation criterion — nothing prevents the COORDINATOR from simply skipping the ADVERSARY step. The role prompt is not enough; there must be a hard gate: before any `🔲` Future item is moved to `queued` state in `docs/aide/queue/current.json`, the COORDINATOR must have written an ADVERSARY evaluation (even a brief one) into the queue entry's `adversary_note` field. The SM must verify this field is non-empty before marking any item as `in_progress`. Without this gate, the ADVERSARY is opt-in and will never be invoked in practice — agents under token-budget pressure always skip optional steps. ⚠️ Inferred from pressure lens: monoculture frame-lock persists because the ADVERSARY is described but never activated.

- 🔲 **Skills inventory as a visible repo artifact: `docs/aide/skills-inventory.md`** — the `Self-improvement tracking` item tracks skill growth rate in the SM batch report. But there is no human-readable artifact in the repo that lists what skills currently exist. A human auditing the system cannot verify whether skills are growing or what patterns have been encoded. Create `docs/aide/skills-inventory.md` as a machine-maintained index: one row per skill (name, description, date added, usage count). The SM's end-of-batch step updates this file when `otherness.learn` runs. Without a visible inventory, the skills library is a black box — the "self-improvement is real" claim cannot be evaluated by a human reading the repo. ⚠️ Inferred from pressure lens: self-improvement cannot be verified without a visible inventory.

- 🔲 **`bash -n` CI guard for `otherness-scheduled.yml` run: blocks is NOT implemented** — doc 13 marks "Workflow step syntax CI-validation" as `✅` (PR #943), but PR #943 fixed a bash syntax error — it did NOT add a `bash -n` CI step. The CI YAML-validation step (`yaml.safe_load`) only catches YAML structure errors, not bash syntax errors inside `run:` blocks. A corrupted `if/else/fi` block will pass `yaml.safe_load` and still fail at runtime. The actual CI guard (extract `run:` blocks, run `bash -n` on each) was described in the Future item in this doc (`Workflow self-healing`) but was not implemented. The 7-consecutive-run failure in April 2026 (PR #943) is evidence that this gap is real and recurring. Add a CI step in `ci.yml` that: (1) parses `otherness-scheduled.yml` with Python; (2) extracts each `run:` script; (3) runs `bash -n` on each; (4) fails CI if any script has a bash syntax error. This must be implemented as a CI gate, not just a pre-merge check. ⚠️ Inferred from pressure lens: loop reliability gap — silent 6-hour outages from bash corruption are not prevented by current CI.

- 🔲 **SM health signal says GREEN but product is not advancing: distinguish "healthy loop" from "fast loop"** — the SM's GREEN/RED/STALL thresholds (defined in the `SM health state definition` item) detect whether the loop is *running*. They do not detect whether it is *delivering high-value work*. A loop can be GREEN for 10 consecutive batches (CI passes, PRs merge, PDCA ≥ 80%) while shipping only minor bug fixes, docs cleanups, and observability tweaks — no new features, no competitive gaps closed. Add a second health dimension: `velocity=FAST|SLOW|STALL` where FAST = at least one item from `docs/design/15-production-readiness.md ## Future` merged in the last 3 batches; SLOW = 3–6 batches since last production-readiness item; STALL = 6+ batches. The SM must report both health dimensions: `loop=GREEN velocity=SLOW`. When `velocity=SLOW`, the COORDINATOR must queue at least one item from `docs/design/15-*.md` in the next batch, not from doc 12/13. The human pressure concern is that GREEN loop + slow product is invisible without this second dimension. ⚠️ Inferred from pressure lens: SM health signal says GREEN but products not advancing fast enough.

- 🔲 **Metrics collected but not acted on: SM must read `docs/aide/metrics.md` and change queue priority** — `docs/aide/metrics.md` is a table of historical batch metrics (PRs merged, test count, CI status, notes). It is updated regularly. It is never read by any agent to *change behavior*. The SM's `health_check` step reads PRs merged in the last batch; it does not read the metrics table to detect trends (e.g., "test count has been flat for 5 batches — the COORDINATOR should queue more test coverage items" or "PRs merged per batch has been declining — something is wrong with the queue depth or item quality"). Add a metrics-trend analysis step to the SM batch report: (1) read the last 5 rows of `docs/aide/metrics.md`; (2) compute trend for test count, PRs merged, and CI pass rate; (3) if test count is flat or declining for 3+ batches, the COORDINATOR must queue a test-coverage item; (4) if PRs merged < 2 for 3+ consecutive batches, post `[METRICS ALERT: delivery declining]`. Without reading the metrics table, the SM is collecting data that influences nothing — the feedback loop exists on paper but not in practice. ⚠️ Inferred from pressure lens: metrics are being collected but not acted on.

- 🔲 **Cross-session prior reasoning persistence: prevent re-litigating settled architecture questions** — the monoculture problem has a second dimension beyond same-model bias: each new session starts with no memory of what prior sessions concluded. A question that was evaluated and settled in session N (e.g., "can krocodile nodes share an ephemeral working directory?" — answer: no, per AGENTS.md canonical example) can be re-opened and answered differently in session N+5 by an agent with a fresh context and the same bad reasoning path. The ADVERSARY role (defined in the `Monoculture break` item) addresses new proposals. It does not address re-evaluation of settled questions. Add a `docs/aide/settled-questions.md` file: a list of architecture questions that have been formally evaluated with reasoning and a conclusion. Before the COORDINATOR queues any item that touches a previously-settled area, it must check this file and reference the settled conclusion in the queue entry. The SM must verify this reference field is non-empty for any item touching a settled area. Without this file, the monoculture frame-lock is addressed in one direction (new proposals) but not the other (re-opening closed questions). ⚠️ Inferred from pressure lens: "the monoculture problem has not been addressed" — the ADVERSARY role alone is insufficient if sessions can forget settled conclusions.

- 🔲 **Future backlog size is uncapped and untracked — vision scan adds faster than queue consumes** — the `docs/design/` directory currently has 78 unqueued `🔲 Future` items across 4 docs (as of 2026-04-21, updated from 58→68→78 as vision scans outpace queue consumption). The vision scan (Step A) adds up to 5 new items per run. The COORDINATOR picks up 2–4 items per batch. At current rates the backlog grows by 1–3 items per batch: the system is accumulating vision debt faster than it is delivering. There is no metric for backlog size, no cap, and no alarm when the backlog exceeds a threshold that makes individual items statistically unlikely to ever ship. Add to the SM batch report: `future_backlog: N items` (count of all `🔲` across `docs/design/`). If `N > 40`, the SM must flag `[BACKLOG OVERFLOW — N unqueued items]` and the COORDINATOR must spend the next batch exclusively burning down backlog items from the oldest docs, not generating new ones. Without this gate, the Future section of every design doc becomes a graveyard of good intentions that no agent ever revisits. ⚠️ Inferred from pressure lens: the system ships PRs every run but the Future backlog never shrinks — reliability requires not just shipping but closing the gap between documented intent and delivered capability.

- 🔲 **Pressure context history is unversioned — no way to audit whether pressure areas were ever acted on** — the vision pressure context (the "Context for this vision scan:" block in `otherness-scheduled.yml`) can be rewritten at any time by an agent. There is no record of what previous pressure contexts said, when they were changed, or whether the items they identified were ever shipped. This means: (1) a pressure area that was acted on can be re-added as a "new" gap by a future vision scan that doesn't know it was already addressed; (2) the pressure can be softened over time by agents that rewrite it to match what they're already doing, rather than raising the bar; (3) a human auditing the system cannot see that the pressure context has drifted from the original human intent. Add a `docs/aide/pressure-history.md` log: every time the pressure block changes (detectable by comparing `git diff otherness-scheduled.yml`), the SM appends the old pressure block with a timestamp to this log. The log is append-only. This makes pressure evolution auditable and prevents silent bar-lowering. ⚠️ Inferred from pressure lens: "Is the loop honest enough?" — without pressure history, the system can redefine honesty to match its own behavior.

- 🔲 **Frame-lock structural break: ADVERSARY must cite a competing design as its null hypothesis** — the `Monoculture break` item introduces an ADVERSARY role, but the ADVERSARY uses the same model and, critically, the same knowledge base. An adversarial session that has only ever seen kardinal will find kardinal-shaped failure modes. The frame-lock is not broken by role-switching alone — it requires external reference frames. Concrete mechanism: the ADVERSARY step in COORDINATOR must, for any architecture decision touching the promotion pipeline, pull the current design decision from either Kargo (`gh api repos/akuity/kargo/contents/docs/...`) or GitOps Promoter and state explicitly: "Kargo solves this by X. Our approach differs because Y. The failure mode of Y that X avoids is Z." If the ADVERSARY cannot articulate Z, the item is not ready to queue. This forces comparison against real alternatives rather than self-referential critique. ⚠️ Inferred from pressure lens: "What would genuinely break the frame-lock?" — the ADVERSARY role exists but uses the same reasoning frame; a structural reference to a competing design is required to escape it.

- 🔲 **Skills usage count will always read zero — no emission mechanism exists** — the `Skills inventory` item (`docs/aide/skills-inventory.md`) includes a `usage_count` column per skill. Skills are markdown files in `~/.otherness/agents/`. There is no mechanism that increments a usage count when a skill is loaded: the `skill` tool in OpenCode loads a skill at runtime but writes nothing back to disk. A `usage_count` column populated by a human (manually incrementing) defeats the purpose; one that is always 0 misleads (implies no skills are being used when they may be). Either: (a) remove `usage_count` from the inventory spec and replace it with `last_cited_in_pr: #NNN` (manually set when a skill demonstrably solved a PR problem — auditable and honest); or (b) add a `[SKILL APPLIED: <name>]` convention to batch reports that the SM's end-of-batch step uses to update the inventory. Without resolving the emission mechanism, the skills inventory is a vanity metric that will never reflect reality. ⚠️ Inferred from pressure lens: self-improvement metrics must be implementable, not aspirational; a usage count with no write path is documentation theater.

- 🔲 **Backlog overflow alarm is unimplemented: SM is supposed to flag at >40 items but never does** — the `Future backlog size` item (this doc) defines `[BACKLOG OVERFLOW — N unqueued items]` when `N > 40`. The current backlog is 78 items (as of 2026-04-21, grown from 68 in the prior note and from 58 before that — backlog grows 1–3 items per batch). The SM has not posted `[BACKLOG OVERFLOW]` once. The alarm exists in documentation but not in the SM's actual batch report logic. The gap: `team.yml` or the SM agent file (`~/.otherness/agents/phases/sm.md`) does not contain the backlog-count step. Adding the Future item to this doc was necessary but not sufficient — the item must also be reflected in the SM's concrete batch-report checklist. This is the pattern that recurs: doc says "SM must do X", SM never does X because the SM agent file was not updated. Until the SM agent file contains: `BACKLOG=$(grep -rc "🔲" docs/design/ | awk -F: '{sum+=$2} END{print sum}'); if [ $BACKLOG -gt 40 ]; then echo "[BACKLOG OVERFLOW — $BACKLOG unqueued items]"; fi` — the alarm is theoretical. ⚠️ Inferred from pressure lens: metrics collected but not acted on — the backlog overflow metric exists in a design doc but has never once fired in production.

- 🔲 **Session token exhaustion is invisible: no detection when agent exits mid-queue due to budget** — when an agent session runs out of token budget mid-batch (typical symptom: queue had 4 items, only 1 was implemented, agent exited without a BATCH COMPLETE report), this failure mode is indistinguishable from a clean session that chose to do less. The SM's health check looks at PRs merged; if 1 PR merged, health=GREEN — even if 3 items were claimed and not implemented, and the session ended abruptly. Add a batch-completion marker: the COORDINATOR writes `session_end: "COMPLETE"` to state at the very last step of a successful batch. If the next session finds `session_end` absent (or set to the previous session's value), it detects the incomplete prior session and posts `[SESSION TRUNCATED — prior batch incomplete, check token budget]` before starting its own queue. The SM must check for this marker in its health_check. Without this, a system that exhausts token budgets on complex items will appear healthy (1 PR merged) while systematically under-delivering (3 items abandoned per run). ⚠️ Inferred from pressure lens: "sessions still fail silently" — token exhaustion mid-queue is the primary silent failure mode not currently detected.

- 🔲 **Pressure context is written by the same agents who evaluate it — conflict of interest** — the current scheduled workflow runs Step A (vision scan, which can write Future items) and then Step B (work execution). The "Context for this vision scan:" block in the workflow is a human-written pressure signal. But agents can also rewrite this block — the `Rewrite vision pressure context` Future item (added by Scan 5 when ≥60% of areas are addressed) explicitly triggers an agent to update it. An agent that rewrites the pressure it is then evaluated against can unconsciously lower the bar. The `pressure-history.md` item detects this drift. But it does not prevent it. The only mechanism that prevents bar-lowering is requiring a human to sign off on any change to the "Context for this vision scan:" block. Add a CI check: `git diff HEAD~ -- .github/workflows/otherness-scheduled.yml | grep "Context for this vision scan" | grep "^+"` — if this outputs non-empty, fail CI with `[HUMAN REVIEW REQUIRED: pressure context changed]`. Agents can still propose the change via PR; the CI failure forces a human to consciously approve the new bar before it merges. ⚠️ Inferred from pressure lens: "Is the loop honest enough?" — an agent that defines its own success criteria and evaluates its own performance is not a reliable evaluator.

- 🔲 **COORDINATOR alphabetic doc ordering starves doc 15 (production-readiness) items permanently** — `coord.md §1c` reads `🔲 Future` items via `sorted(os.listdir('docs/design/'))`. Alphabetic sort means doc 01–14 items are always selected before doc 15 items. With 25 items in doc 12, 11 in doc 13, and 27 in doc 15, the COORDINATOR will never reach a doc-15 item until all doc-12 and doc-13 items are cleared. Doc 15 contains competitive gaps (Kargo parity, enterprise adoption blockers, supply-chain security) — exactly the items that advance the product. Doc 12 and 13 are system meta-items. The current ordering means: "always fix the development process before shipping product features." This is the wrong priority. Fix: in `coord.md §1c` queue-gen, weight doc 15 items 2× (sample at least one doc-15 item per queue of N ≥ 3) and interleave items from different docs using round-robin source selection instead of sorted exhaustion. Without this fix, the SM velocity signal will always read SLOW for product features regardless of loop health — the COORDINATOR is structurally prevented from picking them. ⚠️ Inferred from pressure lens: SM health signal says GREEN but products not advancing fast enough — root cause is queue-gen ordering, not loop reliability.

- 🔲 **Self-feeding queue falls through to `[AI-STEP]` prose when vibe-vision-auto returns 0 items** — `coord.md` runs `vibe-vision-auto.md` when the queue is empty, which runs Scan 3 (code TODO/FIXME inference). When Scan 3 finds no TODOs (as is currently the case — kardinal's codebase has zero TODO/FIXME markers), `vibe-vision-auto` returns 0 new items and the COORDINATOR falls through to: `# [AI-STEP] Read docs/aide/roadmap.md and competitive landscape in vision.md. Identify 3-5 concrete Next items...` — an open-ended AI instruction with no concrete output format, no write path, and no verification. The result is either fabricated issues that don't match the actual Future backlog, or the session stalls silently. The fix: when vibe-vision-auto returns 0 items but `docs/design/*/## Future` contains unqueued `🔲` items (the current state: 64 items), the COORDINATOR should directly create GitHub issues from the existing backlog instead of invoking the `[AI-STEP]` fallback. The backlog is not empty — the routing logic is wrong. ⚠️ Inferred from pressure lens: sessions stall when queues empty — the fallback path is prose-guided, not concrete.

- 🔲 **"Meaningful PR" definition is documented but not machine-checked by the SM at batch end** — the `Loop honesty signal` item (this doc) defines "substantive PR" as one that changes files outside `.otherness/`, `docs/`, `.github/state/`. The SM's health_check computes `HEALTH` and `PROGRESS_CLASS` (ADVANCING/STABLE/STALLED) based on PR count and CI status. It does NOT check whether the merged PRs touched substantive paths. A batch where the only merged PRs are `vision(auto): autonomous scan` (docs-only), `chore(coord): queue update` (state-only), and `fix(workflow): step syntax` (github-only) reports ADVANCING because 3 PRs merged — but all 3 are non-substantive. Implement the check concretely: in the SM batch-report step, count PRs merged in the last 2h that changed at least one file in `pkg/`, `cmd/`, `web/src/`, or `chart/`. If this count is 0, upgrade `HEALTH` from GREEN to AMBER with tag `[SUBSTANTIVE=0]`. The current AMBER upgrade path (STABLE/STALLED) catches no-PR sessions; this item catches PR-merged-but-nothing-real sessions. ⚠️ Inferred from pressure lens: "sessions still produce housekeeping PRs with no real feature content."

- 🔲 **Vibe-vision Scan 3 (TODO inference) has a permanent false-negative: kardinal has no TODO comments** — vibe-vision Scan 3 infers Future items by running `git grep -rn TODO: FIXME: HACK: -- *.go *.ts *.sh`. kardinal's codebase has zero such markers (banned by coding standards or never added). Scan 3 therefore always returns 0 inferred items for kardinal. This means Scan 3 wastes execution time on every Step A run and, worse, silences the "no new inferred items" path that would otherwise trigger the Scan 3 → add-to-doc flow. The design intent was to discover hidden design intent in code comments. A better signal for kardinal's codebase: scan for stub patterns — functions that `return nil, nil`, `return nil`, or `// not implemented` — which are real unimplemented capabilities that should surface as Future items. Replace the TODO/FIXME grep with a stub-pattern scan: `git grep -n "not implemented\|TODO\|return nil, nil" -- pkg/ cmd/` and add matches as inferred Future items. For kardinal, this would surface real gaps rather than always returning 0. ⚠️ Inferred from pressure lens: self-improvement is not real — a scan that always returns 0 is not scanning.

- 🔲 **Vibe-vision Scan 5 pressure-addressed ratio is permanently 0% for a product-shipping project — threshold is miscalibrated** — Scan 5 checks whether pressure areas are "addressed" by matching pressure keywords (e.g., "otherness reliable enough", "the loop honest") against recent merged PR titles. For kardinal-promoter, recent PRs have titles like `feat(cli): extend 'kardinal status'` and `feat(scm): zero-downtime SCM credential rotation` — product PRs that never contain the meta-system keywords Scan 5 searches for. The ratio will always be 0/5 for any project that is actually shipping product features rather than shipping otherness meta-fixes. The 60% threshold never fires. Scan 5 is permanently inert for kardinal. The miscalibration: the design intent of Scan 5 is to detect when the *pressure context is stale* (all gaps addressed, time to raise the bar). For a product-shipping project, staleness is detectable differently: if the Future items added by Scan 1–4 are from the *same categories* as the current pressure context (e.g., multiple items added to doc 12/13 that match the pressure keywords), the pressure context *has* been recognized and addressed in documentation even if the fixes haven't shipped yet. Fix Scan 5: instead of matching against PR titles, match against the *number of new 🔲 items added in the last 5 runs that correspond to each pressure area*. If all 5 pressure areas have ≥1 new 🔲 item added in the last 10 runs, pressure is being processed — add the rewrite trigger. Without this fix, Scan 5 adds zero value for any healthy product-shipping project and should be disabled rather than silently wasting execution time. ⚠️ Inferred from pressure lens: "Is the loop honest enough?" — a scan that is designed to raise the bar but is permanently inert is not honest.

- 🔲 **`cancel-in-progress: true` run-cancellation rate is invisible — hours of Bedrock spend may be silently discarded** — the scheduled workflow uses `concurrency: cancel-in-progress: true`. When a new `workflow_dispatch` or scheduled run starts while a prior run is in progress, the prior run is cancelled at whatever step it was on. If the prior run was 90 minutes into Step B (the main agent execution), all that work is discarded silently. The GitHub Actions UI shows the run as "cancelled" but Issue #1 receives no comment from it (the agent exit hook never fires). The SM's health check looks only at merged PRs from the last 2 hours — a cancelled 90-minute run that produced 0 merged PRs is indistinguishable from a run that simply had nothing to do. Add cancellation visibility: (1) a run that is cancelled mid-Step-B should post a `[SESSION CANCELLED — cancelled at step X after N minutes]` comment to Issue #1 using a GitHub Actions `if: cancelled()` post-step; (2) the SM should check `gh run list --workflow=otherness-scheduled.yml --status=cancelled --limit=5` and if any run was cancelled in the last 3h, include `[CANCELLED RUNS: N]` in the health report. Without this, the system can be in a state where every run cancels the prior one (tight timing, complex PR builds) and the loop appears healthy (hourly cadence, no failures) while producing zero shipping. ⚠️ Inferred from pressure lens: "Sessions still fail silently" — cancellation is a silent failure mode distinct from workflow errors.

- 🔲 **Skill effectiveness measurement: A/B benchmark before vs after a skill is added** — the skills library growth rate is tracked (item above), but whether a new skill *improves agent output quality* is never verified. A skill entry could be syntactically correct, well-structured, and completely ignored by agents in practice. Add a lightweight effectiveness signal: when `otherness.learn` adds a skill, the SM records the error/retry rate for the pattern class the skill targets (e.g., "merge-conflict resolution" → count of merge conflicts requiring >1 retry per batch). After 5 batches, compare the rate before vs after. If the rate did not improve, flag the skill as `⚠️ INEFFECTIVE` in the skills inventory. Without this signal, the skills library is a collection of documentation artifacts, not a measured improvement mechanism. ⚠️ Inferred from pressure lens: "agents are not meaningfully smarter than they were two weeks ago" — growth without quality measurement is vanity.

- 🔲 **Novel pattern detection algorithm: define what "novel" means so the SM can act on it** — the `otherness.learn automatic invocation` item (above) says "SM checks if a novel pattern surfaced this batch and invokes otherness.learn." But there is no defined algorithm for detecting novelty. Without a concrete definition, the SM cannot reliably determine when to invoke learn. Define the algorithm: a pattern is novel if (1) a PR diff introduces a code construct not seen in any prior PR in the last 30 days (structural novelty: new CEL function usage, new krocodile node type, new reconciler pattern), OR (2) an error class recurred 3+ times in a batch before resolution (repetition = teachable moment), OR (3) a `[NEEDS HUMAN]` was resolved in a way that changed a design doc (design evolution = worth encoding). Without a concrete definition, "auto-invoke learn on novel patterns" remains aspirational prose. ⚠️ Inferred from pressure lens: "self-improvement is real" requires a mechanical definition, not just an intention.

- 🔲 **SM directive follow-through verification: confirm COORDINATOR acted on SM queue-priority changes** — the `Metrics collected but not acted on` item says the SM should read `docs/aide/metrics.md` and direct the COORDINATOR to change queue priority. But there is no mechanism to verify the COORDINATOR followed through. The SM posts "prioritize test coverage items" and the COORDINATOR may generate the next queue without reading the SM's directive. Add a verification step: the SM's health check should compare its directive from the prior batch against the COORDINATOR's queue from this batch. If the directive said "prioritize area X" and area X has zero items in the queue, post `[SM DIRECTIVE NOT FOLLOWED — directive: <text>, queue has 0 items from area X]`. Without this, the SM's metrics-to-action feedback loop is broken at the last mile: the SM acts, but cannot confirm the action propagated. ⚠️ Inferred from pressure lens: "metrics are being collected but not acted on" — the missing piece is not the action directive but the verification that the directive was received.

- 🔲 **Vision scan Scan 3/4/5 have no backlog-depth awareness — they add items to an already-overflowing queue** — Scan 3 (TODO inference), Scan 4 (deprecation), and Scan 5 (pressure staleness) add up to 5 new `🔲 Future` items per Step A run without checking whether the existing backlog is already overflowing. Currently (2026-04-21): 78 items unqueued, backlog threshold 40, threshold already exceeded by 195%. Each Step A run can add 5 more items. The irony: the vision scan was designed to ensure work always exists; it is now the primary mechanism making the backlog unmanageable. Add a backlog-gate to Step A: before Scan 3 adds any inferred items, check `BACKLOG=$(grep -rc "🔲" docs/design/ | awk -F: '{sum+=$2} END{print sum}')`. If `BACKLOG > 60`, Scan 3 must skip adding new items entirely and instead emit `[SCAN 3 BLOCKED — backlog at $BACKLOG items (>60 cap); no new items added]`. Scan 4 (deprecation) may still run since it removes items. Scan 5 (pressure rewrite) should only fire if `BACKLOG < 40` — rewriting pressure while backlog overflows adds more vision debt. Without this gate, the vision scan is self-defeating: the more it runs, the larger the graveyard of unshipped ideas. ⚠️ Inferred from pressure lens: "A truly reliable system ships at least one meaningful PR every single run without exception" — an overflowing backlog is the demand-side failure that prevents this guarantee from being implementable.

- 🔲 **Onboarding first-run CI test: scheduled smoke-test that runs the onboarding path end-to-end** — the onboarding first-run metric (tracked above) measures how long setup takes. But no automated test runs the onboarding path on a fresh repo to verify it still works. Onboarding rot is silent: `otherness-config.yaml` schema changes, `hack/install-krocodile.sh` path changes, IAM role name changes — any of these silently break new project setup without breaking existing project CI. Add a scheduled weekly workflow (`onboarding-smoke-test.yml`) that: (1) creates a temporary private repo; (2) runs `/otherness.onboard`; (3) verifies the generated `otherness-scheduled.yml` passes `bash -n` and `yaml.safe_load`; (4) posts `[ONBOARDING SMOKE PASS]` or `[ONBOARDING SMOKE FAIL: <step>]` to Issue #1; (5) deletes the temp repo. Without this, the onboarding "guarantee" is aspirational — a new project added today may still require human debugging of a broken scaffold that CI cannot detect because CI only runs on existing projects. ⚠️ Inferred from pressure lens: "new project added today would still require significant human intervention" — the gap is not documentation, it is the absence of a tested, continuously-verified path.

- 🔲 **Minimum viable reliability: 3 implementation-ready doc-12 items are fully specified but never queued — name them explicitly** — the doc-12 backlog contains dozens of Future items. Many are aspirational design sketches. But exactly three are concrete, implementation-ready bash scripts with no architectural uncertainty that would each reduce the primary pressure-lens gaps by one dimension: (1) `backlog overflow alarm`: SM checks `grep -rc "🔲" docs/design/` and posts `[BACKLOG OVERFLOW — N items]` when N>40 — this is a 10-line shell addition to `~/.otherness/agents/phases/sm.md` with no design ambiguity; (2) `velocity=FAST|SLOW|STALL` health dimension: SM checks whether a doc-15 item merged in last 3 batches — a 20-line shell addition; (3) `predicted_items: N` written to state JSON by COORDINATOR, read by SM to compute simulation delta ratio — a 15-line addition to both `coord.md` and `sm.md`. These three items are not being queued because alphabetic doc ordering puts doc-12 items after doc-01 through doc-11. If they were queued, the pressure-lens failures (GREEN but not advancing, metrics not acted on, simulation not calibrating) would all have detection mechanisms. This item is a pointer, not a new spec: the COORDINATOR should queue items (1), (2), and (3) from this doc before queuing any new doc-12 items. Without naming them explicitly, they will continue to age in the backlog indefinitely. ⚠️ Inferred from pressure lens: all five pressure areas have documented solutions — none have been implemented because the routing mechanism cannot reach them.

- 🔲 **Pressure lens scan produces items but no items are ever queued from it — the vision→queue→ship pipeline is broken at the queue stage** — every pressure lens scan correctly identifies gaps, adds `🔲 Future` items to design docs, and exits. The COORDINATOR, running separately, reads `docs/design/*.md` for Future items but uses alphabetic file ordering that reads doc-01 through doc-14 before doc-15. The result: items added by the pressure lens to doc-12 and doc-13 compete with 54 existing items in those docs; items added to doc-15 are never reached. The vision→queue pipeline has a structural disconnect: vision writes to docs, queue reads from docs in the wrong order. Concrete fix: when a `🔲` item contains the phrase `⚠️ Inferred from pressure lens:`, the COORDINATOR should treat it as a `priority/high` item and queue it ahead of non-pressure items, regardless of doc order. This makes pressure-lens items structurally preferred over organic backlog growth. Without this routing fix, running the pressure lens scan has zero impact on what ships — the loop is not honest because the pressure signal is not connected to the execution path. ⚠️ Inferred from pressure lens: "Is the loop honest enough?" — the answer is no when the human's explicit direction (the pressure context) produces items that the execution path structurally ignores.

- 🔲 **Pressure lens section of Step A has no backlog-depth gate — adds items regardless of overflow** — the "Vision scan Scan 3/4/5 have no backlog-depth awareness" item (this doc) adds a gate for Scans 3–5. The human-authored pressure context section of Step A (the "Context for this vision scan:" block) is a separate code path that is not gated: it runs unconditionally and can add items even when the backlog is at 195% of threshold (77 items vs 60-item cap, 2026-04-21). The `vibe-vision-auto.md` pressure lens section must read the same backlog depth before adding items: if `BACKLOG > 60`, emit `[PRESSURE LENS BLOCKED — backlog at $BACKLOG items; identifying gaps only, not adding new items]` and output a human-readable summary of the gaps found without writing them to design docs. The pressure lens findings are still valuable when the backlog overflows — they should be posted as a comment to Issue #1 for human awareness, not written as new `🔲` items that will never be queued. ⚠️ Inferred from pressure lens: "A truly reliable system ships at least one meaningful PR every single run without exception" — writing items to a graveyard is not shipping.

- 🔲 **Backlog triage mode: when backlog > 60, Step A should create GitHub issues from existing items instead of adding more** — the self-feeding queue item (this doc) describes the COORDINATOR creating issues from the backlog. But the COORDINATOR runs only in Step B. When the backlog overflows and Step A detects it, Step A is the right place to trigger triage: instead of adding new `🔲` items, Step A should call `gh issue create` for the top 3 oldest untracked `🔲` items (those with no corresponding open issue), converting design-doc intent into actionable GitHub issues. This is concrete work that reduces the backlog-to-queue gap without adding more backlog. The items to select: the oldest 3 `🔲` items in docs 12–15 that do not have a matching open issue title (match first 40 chars). Each issue created removes one item from the "undiscoverable graveyard" category. Without this mode, overflow detection is a warning with no consequence — the graveyard grows regardless. ⚠️ Inferred from pressure lens: the vision scan should be a net reducer of backlog debt when overflow is detected, not a net adder.

- 🔲 **vibe-vision-auto adds items to a broken system: PDCA status not checked before Step A runs** — PDCA has been failing (3 failures: 2026-04-21 03:10, 2026-04-20 15:08, 2026-04-20 15:04) while `vibe-vision-auto` ran normally, added items to the backlog, and reported "GREEN" in its completion comment. The vision scan agent does not check whether the system it is scanning is currently passing its own end-to-end tests. This is the wrong priority: adding new capability items to a system whose existing capabilities are broken is actively harmful — engineers implement new items, the underlying journey failures remain unaddressed, and the product regresses further. Add a Step 0 to `vibe-vision-auto.md` (before Scan 1): `PDCA_STATUS=$(gh run list --repo $REPO --workflow=pdca.yml --limit 1 --json conclusion --jq '.[0].conclusion' 2>/dev/null)`. If `PDCA_STATUS == "failure"`, emit `[VIBE-VISION-AUTO] ⚠️ PDCA FAILING — last run failed. Scanning for regression cause instead of adding backlog items.` and run only Scan 2 (stale check) — skip Scans 3–5 and the pressure lens additions entirely. Post the PDCA failure status to Issue #1 with `needs-human` label. Without this gate, the vision scan is anti-correlated with system health: it adds the most new items precisely when the system is broken and needs the least new direction. ⚠️ Inferred from pressure lens: "Is otherness reliable enough?" — a vision scan that ignores active test failures is not a reliable loop component.

- ✅ **PDCA failure enforcement in COORDINATOR — §1c-pdca-gate + SM §4f-regression attribution**: COORD §1c-pdca-gate now reads `pdca_status` from state.json before queue generation and item claiming. When `pdca_status=failure`, queue generation is skipped entirely, item claiming is skipped, and one COORDINATOR HALTED comment is posted to REPORT_ISSUE. SM §4f-regression detects the success→failure transition via PDCA comment parsing, runs `git log` attribution over `pkg/`, `cmd/`, `chart/`, `web/src/` since last git tag, posts `[PDCA REGRESSION]` with attributed commits to REPORT_ISSUE, and opens a dedup-guarded needs-human issue. `pdca_regression_posted` flag prevents re-posting on every SM cycle while regression persists. Cleared when PDCA passes again. (PR #1061, 2026-04-22)

- 🔲 **Health snapshot shows PDCA status but does not drive loop color to RED — GREEN is a lie when PDCA fails** — the health snapshot comment (PR #1058) shows `pdca: X/Y` as a separate field. When PDCA fails, the snapshot can show `loop: GREEN | pdca: 0/7` simultaneously — presenting a contradictory state where the loop is "healthy" while the product's end-to-end tests are broken. The `loop` color must be driven by PDCA status, not just by PR merge counts. Concrete fix: in SM §4f-health-snapshot, if `PDCA_STATUS=failure` (from state.json), force `loop_health=RED` regardless of PR count. The health snapshot GREEN/AMBER/RED state must satisfy: `loop=RED` ⟺ workflow failed OR PDCA failing. `loop=GREEN` ⟺ workflow ran AND PDCA passing AND at least one substantive PR merged. `loop=AMBER` ⟺ workflow ran AND (PDCA unknown OR no substantive PRs). The current implementation cannot produce `loop=RED` from PDCA failure alone because the color computation is disconnected from the PDCA status field. A human reading the health snapshot and seeing `loop: GREEN` while PDCA has been failing for 72 hours is being actively misled. ⚠️ Inferred from pressure lens: "Is the loop honest enough?" — a health signal that reports GREEN during active end-to-end test failures is the definition of an dishonest loop.

- 🔲 **Consecutive PDCA failure escalation is absent: 3+ failures must trigger `[NEEDS HUMAN]` not just attribution** — the PDCA regression attribution item (doc 12 line 128) posts `[PDCA REGRESSION]` on the first PDCA→failure transition. It does not define what happens when PDCA has been failing for 3, 5, or 10 consecutive runs. The system reaches a state of chronic failure where: the attribution was posted once, the regression was never resolved, and subsequent runs keep posting the same attribution without escalating. After 3 consecutive PDCA failures, the SM must post a `[NEEDS HUMAN: PDCA regression unresolved for 3+ runs]` issue with `needs-human` label (not just a comment to Issue #1). This is distinct from the initial attribution — it is an escalation, not a duplicate. The SM's health_check must track `consecutive_pdca_failures` in state.json (increment on failure, reset on success). When this counter reaches 3, open a `needs-human` issue with the failing journey names and the list of loop-shipped commits since last PDCA pass. When this counter reaches 7, the SM should reduce the cron cadence to 24h (to avoid burning Bedrock budget on a broken system) and post `[LOOP SUSPENDED — PDCA failing for 7+ runs, reduced to 24h cadence]`. Without this escalation path, the loop will continue running indefinitely on a broken system until a human happens to notice — which is the opposite of autonomous reliability. ⚠️ Inferred from pressure lens: "Is otherness reliable enough? Sessions still fail silently" — a system that reports GREEN while its own tests have been failing for days is failing silently, just with extra steps.

- 🔲 **Backlog overflow alarm is above threshold right now (74 items) and has never fired — alarm is dead code** — the `Future backlog size` item (this doc) defines `[BACKLOG OVERFLOW — N unqueued items]` when N>40. The current backlog is 74 items (confirmed 2026-04-22). The SM has never posted this alarm. The SM agent file (`~/.otherness/agents/phases/sm.md`) was not updated when this Future item was written. The alarm is dead documentation. This is the pattern described in the `SM directive follow-through verification` item — doc says "SM must do X", SM never does X. Immediate consequence: the vision scan keeps adding items to a 74-item backlog (doc 12 alone has 45 items) while the COORDINATOR can only process 2–4 per batch. At this ratio, items added in April 2026 will not ship until late 2027 unless something changes. The SM must post `[BACKLOG OVERFLOW — 74 unqueued items: 45 in doc-12, 14 in doc-13, 10 in doc-15, 3 in doc-39, 2 in doc-06]` immediately. The vibe-vision-auto backlog gate (Scan 3 blocked when >60 items) must also be enforced: current run confirms Scan 3 returned 0 items — not because the gate fired, but because the codebase has no TODO markers. The gate was never tested under overflow conditions. ⚠️ Inferred from pressure lens: "Is the loop honest enough?" — an overflow alarm that has never fired while the backlog is 85% above its trigger threshold is not a monitoring system; it is documentation theater with real consequences (backlog grows unboundedly, items never ship).

---

## Zone 1 — Obligations

**O1 — The autonomous merge protocol is attempted before any `[NEEDS HUMAN]` post.**
No PR may be labeled `[NEEDS HUMAN: pr-approval-required]` until the agent has tried:
(a) `--admin` merge, (b) enforce_admins disable + merge. If (b) fails with 403, that
is valid scenario 1. Log the specific HTTP error in the [NEEDS HUMAN] post.

**O2 — `[NEEDS HUMAN]` posts are unique per PR, not per cycle.**
If the agent already posted `[NEEDS HUMAN]` for PR #N in a previous cycle, it does
not post again in the next cycle. It checks existing comments before posting.

**O3 — Queue generation is gated by in-review count.**
If in-review items >= 3 and none can be merged (scenario 1 confirmed), no new
work items are generated. The session enters standby.

**O4 — enforce_admins is always restored.**
Any session that sets enforce_admins=false must set it back to true before the
bash block exits — including on failure. This is non-negotiable.

---

## Zone 2 — Implementer's judgment

- Whether to gate at 3 or a different number: 3 is correct for single-operator projects.
  A human typically reviews 1-3 PRs per sitting. Beyond 3 queued, they stop reviewing.
- Whether to apply this to all PR types: yes. There is no "trivial" PR that justifies
  a different merge strategy.
- Whether the agent should comment on PRs to indicate they're waiting: no. The
  `[NEEDS HUMAN]` issue label is sufficient. Comments create noise.

---

## Zone 3 — Scoped out

- Projects with multiple required reviewers (enterprise branch protection)
- Merge queue workflows (branch protection using merge queues instead of direct merge)
- Dependabot PRs (the agent does not handle automated dependency PRs)
